# Recursive-Fractal-Tree
A fractal tree is a self-similar recursive structure: each branch spawns two child branches rotated by a fixed angle and scaled down by a constant factor. Repeating this rule from a single trunk produces an organic, tree-like form.

The implementation below was originally written using Python's turtle module and then translated into an interactive animated visualization using React, SVG, and Framer Motion.
